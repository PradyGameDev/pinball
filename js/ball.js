class Ball extends Component {
    constructor(position, mesh) {
        super(position, mesh);
        mesh.castShadow = true;
    }

    update() {
        // Apply gravity
        // Check for collision
    }
}